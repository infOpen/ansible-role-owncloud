---

# Tests for owncloud role

- hosts: localhost
  remote_user: root
  roles:
    - "infOpen/ansible-role-owncloud"
  post_tasks:
    - include: ./test_common.yml
  vars:
    owncloud_webserver_cn: foobar

